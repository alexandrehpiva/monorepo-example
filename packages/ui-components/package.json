{
  "name": "@monorepo/ui-components",
  "version": "0.0.0",
  "description": "UI Components",
  "main": "dist/index.js",
  "module": "dist/index.es.js",
  "types": "typings/ui-components/index.d.ts",
  "directories": {
    "lib": "lib",
    "test": "__tests__"
  },
  "files": [
    "dist",
    "typings"
  ],
  "scripts": {
    "test": "jest",
    "build": "tsc -b",
    "watch": "tsc -b -w --preserveWatchOutput",
    "clean": "rm -rf ./dist && rm -f tsconfig.tsbuildinfo && rm -rf node_modules"
  },
  "peerDependencies": {
    "react": ">=16.8.0",
    "react-dom": ">=16.8.0"
  }
}
