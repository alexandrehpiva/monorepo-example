{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --stream",
    "clean": "lerna run clean --parallel",
    "watch": "tsc -b -w --preserveWatchOutput"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
